/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true, // Optional: enables faster minification using the SWC compiler
  }
  
  export default nextConfig;
  